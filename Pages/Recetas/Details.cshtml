@page "{id:int}"
@model Clinica.Pages.Recetas.DetailsModel
@{
    ViewData["Title"] = "Detalle de la Receta";
}

<div class="container mt-4 animate__animated animate__fadeIn">
    <h2 class="mb-3">Detalle de la Receta</h2>
    <div class="card shadow-sm">
        <div class="card-body">
            <h5 class="card-title">@Model.Receta.Medicamento</h5>
            <p class="card-text">
                <strong>Fecha:</strong> @Model.Receta.Fecha.ToShortDateString()<br />
                <strong>Duración:</strong> @Model.Receta.DuracionDias días<br />
                <strong>Descripción:</strong> @Model.Receta.Descripcion
            </p>
            <a href="/Recetas" class="btn btn-secondary rounded-pill">Regresar</a>
        </div>
    </div>
</div>
